{
  "tests": [
    {
      "name": "Get profile without auth fails",
      "method": "GET",
      "path": "/",
      "expectedStatus": 401,
      "expectedBody": {
        "error": "Требуется авторизация"
      },
      "bodyMatcher": "partial"
    },
    {
      "name": "Update profile without fields returns error",
      "method": "PUT",
      "path": "/",
      "headers": {
        "X-Authorization": "Bearer invalid"
      },
      "body": {},
      "expectedStatus": 401,
      "bodyMatcher": "partial"
    }
  ]
}