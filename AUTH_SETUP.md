# Настройка авторизации ConnectHub

На платформе настроены четыре способа входа:
1. **Email + Пароль** — работает сразу
2. **VK ID** — требует настройки VK приложения
3. **Яндекс ID** — требует настройки Яндекс OAuth
4. **Google ID** — в разработке

---

## ✅ Email авторизация

**Статус:** Полностью работает

Пользователи могут:
- Регистрироваться через email и пароль
- Входить в аккаунт
- Восстанавливать пароль (если настроен SMTP)

**Что настроено:**
- Backend функция: `https://functions.poehali.dev/3aed2af4-8257-4d82-b13a-3ffe059d8854`
- База данных: таблица `users` с полями для всех провайдеров
- JWT токены для безопасности

---

## 🔵 VK ID авторизация

**Статус:** Требует настройки VK приложения

### Как подключить VK ID:

#### Шаг 1: Создание VK ID приложения

⚠️ **Требуется подтверждение профиля через ГосУслуги + ИНН**

1. Перейдите на [id.vk.com/about/business/go](https://id.vk.com/about/business/go)
2. Нажмите **"Подтвердить профиль"** (нужны ГосУслуги + ИНН)
3. После подтверждения нажмите **"Добавить приложение"**
4. Введите **название приложения** (минимум 3 символа): `ConnectHub`
5. Выберите платформу **Web**
6. Загрузите иконку приложения
7. Добавьте **базовый домен**: ваш домен без `https://` (например: `yourdomain.com`)
8. Добавьте **Redirect URL**: `https://yourdomain.com/auth/vk/callback`
9. Нажмите **"Создать приложение"**
10. Скопируйте **ID приложения** и **Защищённый ключ**

#### Шаг 2: Добавление секретов в проект

После получения ID и ключа, добавьте секреты через интерфейс poehali.dev:

1. `VK_CLIENT_ID` — ID приложения из VK
2. `VK_CLIENT_SECRET` — Защищённый ключ из VK
3. `VK_REDIRECT_URI` — `https://yourdomain.com/auth/vk/callback`

#### Шаг 3: Тестирование

⚠️ **Важно:** VK авторизация **НЕ работает в iframe редактора**!

Для проверки:
1. Опубликуйте сайт
2. Откройте в **отдельной вкладке браузера** (не в редакторе)
3. Нажмите **"Войти через VK"**
4. Авторизуйтесь через VK ID
5. Вас перенаправит на `/auth/vk/callback`, затем в `/profile`

---

## 🟡 Яндекс ID авторизация

**Статус:** Требует настройки Яндекс OAuth приложения

### Как подключить Яндекс ID:

#### Шаг 1: Создание Яндекс OAuth приложения

1. Перейдите на [oauth.yandex.ru](https://oauth.yandex.ru/)
2. Нажмите **"Создать приложение"**
3. **Название вашего сервиса**: `ConnectHub`
4. **Платформы**: выберите **"Веб-сервисы"**
5. **Права доступа** (в разделе "Основные"):
   - ✅ Доступ к адресу электронной почты
   - ✅ Доступ к логину, имени и фамилии, полу
   - ✅ Доступ к портрету пользователя
6. **Redirect URI**: `https://yourdomain.com/auth/yandex/callback`
7. Скопируйте **ClientID** и **Client secret**

#### Шаг 2: Добавление секретов в проект

После получения ClientID и Client secret, добавьте секреты через интерфейс poehali.dev:

1. `YANDEX_CLIENT_ID` — ClientID из Яндекс OAuth
2. `YANDEX_CLIENT_SECRET` — Client secret из Яндекс OAuth
3. `YANDEX_REDIRECT_URI` — `https://yourdomain.com/auth/yandex/callback`

#### Шаг 3: Тестирование

⚠️ **Важно:** Яндекс авторизация **НЕ работает в iframe редактора**!

Для проверки:
1. Опубликуйте сайт
2. Откройте в **отдельной вкладке браузера** (не в редакторе)
3. Нажмите **"Войти через Яндекс ID"**
4. Авторизуйтесь через Яндекс ID
5. Вас перенаправит на `/auth/yandex/callback`, затем в `/profile`

---

## 🔴 Google ID

**Статус:** Кнопка добавлена, но функционал не подключен (disabled)

Для активации потребуется:
- Установка расширения `google-auth`
- Настройка OAuth приложения в Google Cloud
- Добавление секретов в проект

---

## Как работает единая авторизация

### Автоматическая привязка аккаунтов

Система автоматически связывает аккаунты по email:

**Сценарий 1:** Пользователь зарегистрировался через Email
- Создаётся аккаунт с email и паролем
- При входе через VK с тем же email → VK привязывается к существующему аккаунту

**Сценарий 2:** Пользователь зарегистрировался через VK
- Создаётся аккаунт с `vk_id` и email (если VK предоставил)
- При входе через Email с тем же email → пароль устанавливается для существующего аккаунта

**Сценарий 3:** VK не предоставил email
- Создаётся аккаунт с уникальным email: `vk_{vk_user_id}@vk.local`
- Пользователь может войти только через VK

### Структура базы данных

Таблица `users` поддерживает все провайдеры:

```sql
-- Общие поля
id, email, name, avatar_url, created_at, updated_at, last_login_at

-- Email авторизация
password_hash, email_verified, failed_login_attempts, last_failed_login_at

-- VK авторизация
vk_id

-- Для будущих провайдеров
google_id, yandex_id
```

### Хранение токенов

Каждый провайдер использует **собственные ключи** в localStorage:

- Email: `access_token`, `refresh_token`
- VK: `vk_auth_refresh_token`, `vk_auth_code_verifier`, `vk_auth_state`
- Google: `google_auth_refresh_token` (когда будет подключен)
- Яндекс: `yandex_auth_refresh_token` (когда будет подключен)

**Нет конфликтов!** Пользователь может войти через любой провайдер с одним email.

---

## Backend функции

| Провайдер | URL | Статус |
|-----------|-----|--------|
| Email | `https://functions.poehali.dev/3aed2af4-8257-4d82-b13a-3ffe059d8854` | ✅ Работает |
| VK | `https://functions.poehali.dev/2494c44f-f6f6-40ff-871c-fb7e09d9702d` | ⚙️ Требует настройки |
| Яндекс | `https://functions.poehali.dev/635fdfd0-fce3-46f9-a566-18d196e6e486` | ⚙️ Требует настройки |

---

## Страницы

| Путь | Описание |
|------|----------|
| `/login` | Страница входа со всеми провайдерами |
| `/register` | Страница регистрации со всеми провайдерами |
| `/auth/vk/callback` | Обработка VK редиректа |
| `/auth/yandex/callback` | Обработка Яндекс редиректа |
| `/profile` | Профиль после успешного входа |

---

## FAQ

**Q: Почему VK авторизация не работает в редакторе?**  
A: VK блокирует работу в iframe по соображениям безопасности. Тестируйте только на опубликованном сайте.

**Q: Что делать, если пользователь входит через VK без email?**  
A: Система создаст уникальный email `vk_{id}@vk.local`. Пользователь сможет войти только через VK.

**Q: Можно ли привязать несколько провайдеров к одному аккаунту?**  
A: Да! Если email совпадает, все провайдеры автоматически привяжутся к одному аккаунту.

**Q: Как добавить Google?**  
A: Установите расширение `google-auth` через `extension_manager` и настройте OAuth приложение в Google Cloud.

---

## Следующие шаги

1. ✅ Email авторизация — работает сразу
2. ⚙️ VK ID — настройте VK приложение и добавьте секреты
3. ⚙️ Яндекс ID — настройте Яндекс OAuth и добавьте секреты
4. 🔜 Google — установите расширение `google-auth`